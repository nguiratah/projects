%while and(and(size_window*(j+1)+floor(K/size_window)<=K , m<N) ,x<K) 
%i = (j)*size_window;
%X = m;
 %   while and(and(i<size_window*(j+1)+floor(K/size_window), m<N), x<m+size_window)
  %      i = i+1;
   %     z = rand(1);
    %    if z>P
     %       m = m+1;
      %  end
       % for b =(size_window)*j+1 : length(T)
        %    T(b) = T(b)-1;
        %end
    %end
%j = j+1;
%end